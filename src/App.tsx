import React from 'react';
import { AppLayout } from './template';

export const App: React.FC = () => {
  return (
    <AppLayout
      appTitle="LOGO / MARCA (CABECERA SUPERIOR)"
      appBadge="BADGE SUPERIOR"
    />
  );
};

export default App;
